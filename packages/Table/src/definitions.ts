export interface Config {
    pagination ?: boolean
    pageIndex ?: number
    pageSize ?: number
    layout ?: string
    className ?: string
    total ?: number
    index ?: boolean
    selection ?: string
    'row-key' ?: string
}