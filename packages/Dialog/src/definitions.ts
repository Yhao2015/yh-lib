export interface Config {
    visible : boolean
    title ?: string
    width ?: number
    loading ?: boolean
    footer ?: boolean
    isSave ?: boolean
    saveText ?: string
    cancelText ?: string
    customClass ?: string
}